// Clear up the console
clear

echo "----- Booting configurations"
echo "Please Wait..."

// Reset debug system
adsp_debug 0

// I am a developer but you can find logs annoying
developer 0

// Loading Aliases
echo "----- Loading Aliases"
exec main/alias

// Exec Binds
echo "----- Binding of Counter-Strike" 
exec main/bind

// Change here the default xAir
echo "----- Painting Crosshair"
exec main/defaultcross

// Exec POV settings
echo "----- Training bots with the new POV"
exec main/pov

// Exec HUD style
echo "----- HUD for a Global Elite"
exec main/hud

// Exec inputs properties
echo "----- Changing inputs"
exec main/input

// Exec joy commands
echo "----- Disabling peasantry"
exec main/joy

// Exec Sounds settings
echo "----- Sound System Loaded!" 
exec main/sound

// Exec Connections stuff
echo "----- Networking configurated"
exec main/network

// Exec Monitoring game
echo "----- Add monitoring configurations"
exec main/monitoring

// Exec Performance
echo "----- Performance cfg loaded"
exec main/performance

echo ".:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:."
echo ".                                                                     ."
echo ".                   CONFIGURATIONS LOADED HAVE FUN                    ."
echo ".                                                                     ."
echo ".:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:."